'use strict';

let binaryTree = require('./binary_tree');

let db = (f) => {
    return () => {

        let argvs = [].slice.call(arguments, 0);

        let tree =  binaryTree.BinaryTree();
        
        
        
    };
};

module.exports = db;


